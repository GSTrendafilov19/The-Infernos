#include <iostream>
#include <string> //For string methods
#include "Menu.h"
using namespace std;

// this is the main function
int main() {
	string arrow = "-->";
	int arrowPos = 0;
	chooseMenu(arrow, arrowPos);
}